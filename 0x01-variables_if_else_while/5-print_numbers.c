#include <stdio.h>
/**
  *main - Entry point
  *Description: 'print numbers of base of 10'
  *Return: always 0 means good
  */
int main(void)
{
	int p;

	for (p = 0; p <= 9; p++)
	{
		printf("%d", p);
	}
	printf("\n");
	return (0);
}
